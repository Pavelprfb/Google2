<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/style.css">
  <title>User List</title>
</head>
<body>
  <h2>All Users</h2>
  <ul>
    <% users.forEach(u => { %>
      <li><a href="/view/<%= u.email %>"><%= u.email %></a></li>
    <% }) %>
  </ul>
</body>
</html>